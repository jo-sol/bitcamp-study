package com.eomcs.pms.handler;

public interface Command { 
  void execute();

}

// 인터페이스 객체 사용 규칙 통일하기
// 핸들러마다 add, list, detail 이런식으로 메서드 이름이 다 달라서 
// app 클래스에서 메서드를 사용할 때마다
// 이름을 외워서 쳐야하니깐
// Command 인터페이스를 만들어서 execute로 메서드 이름 통일 시키면 편리함!